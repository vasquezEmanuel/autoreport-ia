NODE_ENV=development
PORT=3000

# Genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<CAMBIAR_POR_STRING_ALEATORIO_MIN_32_CHARS>
JWT_EXPIRES_IN=24h

# API de Inteligencia Artificial (obtener en console.anthropic.com)
ANTHROPIC_API_KEY=<CAMBIAR_POR_TU_API_KEY>

# CORS — origen del frontend
CORS_ORIGIN=http://localhost:5173